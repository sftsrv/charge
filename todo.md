# To-Do

- [ ] Implement bundling plugin (e.g. <https://parceljs.org/> support)
- [ ] Some built-in themes
- [ ] Provide simpler running API (e.g. run -> await -> match -> panic or ok.)
- [ ] Rework the composition functions in terms of other existing compositions
- [ ] Dev server with:
  - [ ] Automatic caching of images
  - [ ] File watching/pipeline runs
  - [ ] Cached asset builds (by linking to a per-asset render function?) - may need to think about how the render function is passed, loaders might need to expose a per-asset loader or something that can be used to re-render a single asset

